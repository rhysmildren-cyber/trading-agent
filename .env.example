# Alpaca paper trading (generate at https://app.alpaca.markets — use PAPER keys only)
APCA_API_KEY_ID=
APCA_API_SECRET_KEY=

# Supabase (project: trading-agent). Use the service_role key — server-side only, never commit.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
